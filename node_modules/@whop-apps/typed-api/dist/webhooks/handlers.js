"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "transformToCamelCase", {
    enumerable: true,
    get: function() {
        return transformToCamelCase;
    }
});
function transformToCamelCase(str) {
    if (!str) return str;
    const [first, ...parts] = str.split(/[-_\.]/);
    const uppercased = parts.map((part)=>part ? part[0].toUpperCase() + part.slice(1) : part);
    return [
        first,
        ...uppercased
    ].join("");
}
