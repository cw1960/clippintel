import { type MethodHandlers } from "./handlers";
export type WebhookHandlers<Schema> = {
    [K in keyof Schema[keyof Schema] as K extends string ? `${K}Handler` : never]: (req: Request, handlers: MethodHandlers<Schema, K>) => Promise<Response>;
};
export declare function createWebhookHandler<Schema>({ signatureHeaderName, signatureKey, }: {
    signatureHeaderName: string;
    signatureKey: string;
}): WebhookHandlers<Schema>;
//# sourceMappingURL=index.d.ts.map